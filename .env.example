# API mode: "mock" uses the in-memory adapter + fixtures; "real" hits the NestJS backend.
VITE_API_MODE=mock
VITE_API_BASE_URL=http://localhost:3000

# Google Maps JS API key (MapView wrapper). Leave blank in mock mode — MapView shows a placeholder.
VITE_GOOGLE_MAPS_KEY=
